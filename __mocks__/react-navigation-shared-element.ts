import { createStackNavigator } from '@react-navigation/stack';
import { View } from 'react-native';

export const createSharedElementStackNavigator = createStackNavigator;
export const SharedElement = View;